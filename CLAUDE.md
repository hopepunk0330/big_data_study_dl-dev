# 프로젝트 안내

## 개요
빅데이터 수업 과제로 시작한 캐글 Mercari Price Suggestion(중고거래 판매자 가격 제안, 회귀) 프로젝트를 PM 포트폴리오로 확장한 저장소. 두 워크스트림을 함께 진행한다.

- **ML 파이프라인** (`ML/`): EDA → 전처리 → 모델 4종(Ridge/LightGBM/XGBoost/CatBoost) 비교 → 하이퍼파라미터 최적화 3종(GridSearchCV/RandomizedSearchCV/Optuna) 비교 → 평가(RMSLE). 베이스라인은 Ridge/LightGBM 참고 노트북(RMSLE 0.4570), 목표는 이보다 낮은 RMSLE.
- **PM A/B 테스트**: ML 분석에서 나온 인사이트를 "가격을 단일가로 단정할 것인가, 범위로 유연하게 제시할 것인가"라는 UX 실험으로 검증한다. 참여자 화면(순수 HTML/CSS/JS, Vercel 배포: https://01-ml-mercari-price-2608.vercel.app — 2026-08-28 배포, `web/index.html`이 정식 소스, GitHub main 브랜치 push 시 자동 재배포)과 백오피스(`app/`, Streamlit, Streamlit Community Cloud 배포: https://01-ml-mercari-price-2608.streamlit.app/ — 2026-08-28부터 비밀번호 게이트 제거, 공개 접근)로 구성(2026-08-24 스택 분리 — Streamlit 프로토타입에서 컴포넌트 간 HTML 미연결·좁은 화면 컬럼 자동 스택·iframe JS 격리 등 구조적 한계가 실제로 재현돼, 참여자 화면만 분리). Amplitude(정량)·Microsoft Clarity(정성) 연동, 분석은 이항검정 + Wilson 신뢰구간(1차 지표) + McNemar 검정(보조 지표, 가격 수용도).

진행상황·의사결정 기록과 세부 계획(과제 스펙, 실험 설계, 모델 비교표 등)은 로컬 문서가 아니라 **Notion 프로젝트 페이지**(https://app.notion.com/p/1-mercari-price-3c2ebec5c35780aab053c2aa8d25a6fa, "1차) 빅데이터 분석 - 캐글 mercari price")에 있다 — 세부 계획을 확인하거나 바꿀 때는 그쪽을 먼저 본다. `docs/planning/`에 새 문서를 만들거나 버전업하면 이 링크로 자동 동기화한다(매번 링크를 다시 찾지 않는다).

**결과보고서(22번)·발표자료(24번)의 확정 제목(2026-08-26 Notion 메인 페이지 "연구 방향 표현 문구" 절에서 확정, 이후 변경 없음)**: "머신러닝 기반 가격 예측에서 불확실성 표현 방식(점추정 vs 구간추정)이 사용자 신뢰에 미치는 영향: Mercari 데이터 기반 파일럿 연구" — 표지·목차 등 어디에 쓰든 이 문장을 그대로(요약·재구성하지 않고) 쓴다. 표지에서 제목을 여러 줄로 쪼개 배치하는 것 자체는 괜찮지만(예: "…불확실성 표현 방식" / "…사용자 신뢰에 미치는 영향" / "점추정(단일가) vs 구간추정(범위) — Mercari 데이터 기반 파일럿 연구"), 괄호 안 "(점추정 vs 구간추정)"과 콜론 뒤 "Mercari 데이터 기반 파일럿 연구"를 빠뜨리거나 다른 표현으로 바꾸면 안 된다 — 전체 문장이 어딘가에는 정확히 다 보여야 한다. 이 보고서·발표자료의 세부 소재(왜 이 방법론을 택했는지, HPO 재실행 히스토리, comps vs ML 모델 관계 등 발표에 쓸 논지)는 `docs/planning/report_notes.md`("보고서 포인트 노트", 대화 중 나온 아이디어를 모아두는 running 메모)에 정리돼 있다 — 22/24 작성·재작업을 지시할 때마다 이 파일을 먼저 읽고 반영한다.

`.claude/`와 `docs/harness/`에는 pandas_dev → ml-dev를 거쳐 이식된 재사용 멀티에이전트 하네스가 있다 — 도메인별 전문가 에이전트(`ml-expert` 등, 최상위 전문가 페르소나 — 명시적 요청 시 완성된 구현까지 직접 수행), 크로스도메인 검증 에이전트(`qa-reviewer`), 하네스 자체 정합성 감사 에이전트(`data-harness-auditor`)는 `.claude/agents/`에, 사람이 직접 읽고 관리하는 공용 규칙·레퍼런스 문서는 숨김 폴더가 아닌 `docs/harness/`에, 노트북 스타일 가이드는 `.claude/skills/notebook-toolkit`에 둔다. 포크 계보는 `docs/harness/HARNESS_LOG.md` 참고.

## 폴더 구조
```
ML/
  01_eda.ipynb              # 진단: 가격 분포, 카테고리/브랜드 분포, 결측치
  02_preprocessing.ipynb    # 규칙: 전처리 + 3-way split → data/processed/에 결과 저장(joblib)
  03_model_ridge.ipynb      # 대조: 저장된 전처리 결과를 불러와 학습 (넷 다 같은 번호 = 같은 단계의 병렬 비교)
  03_model_lightgbm.ipynb
  03_model_xgboost.ipynb
  03_model_catboost.ipynb
  04_hpo_comparison_round{N}.ipynb  # 승자 모델 튜닝 3종(Grid/Random/Optuna) 비교 — 접미사 없는 대표 사본은 두지 않고, 가장 큰 N이 현재 최종본(round 재실행 규칙: `docs/harness/ml-project-workflow.md` 15절)
  05_final_model_round{N}.ipynb     # 최종 모델 확정, test 평가, SHAP, 모델+전처리기 저장 — 위와 동일 규칙
app/      # A/B 테스트 백오피스(Streamlit) + 통계 함수(pytest 대상) — 참여자 화면(HTML/CSS/JS)은 별도 위치, Vitest 대상
analysis/ # AB테스트·백오피스·백엔드 데이터에 대한 판다스 기반 결과 분석 노트북(classical ML 파이프라인이 아닌 것) — ML/과 대등한 위치, pandas-expert 담당(2026-08-31 신설, 그 전엔 app/에 있었음)
data/     # 원본·전처리 데이터 (.gitignore 처리, 커밋하지 않음)
docs/     # 하네스(harness/) + PM 문서
```
전처리(02)는 한 번만 돌리고 결과를 저장, 모델별 노트북(03_model_*)은 그 결과를 불러와 재사용한다 — 모델마다 전처리를 새로 돌리는 시간·메모리 낭비를 막기 위함.

`analysis/`의 노트북은 `app/`(백오피스 코드·통계 함수)를 상대경로(`../app/`)로 참조한다 — 물리적으로는 별도 폴더지만 논리적으로는 AB테스트 워크스트림의 일부다.

## 개발 환경
- 가상환경: **conda** (이 학습 시리즈 관례 — ML은 Anaconda, DL은 venv)
- Python 버전: **3.11** (최신 버전을 기본값으로 삼지 않는다 — LightGBM/XGBoost/CatBoost 등 ML 라이브러리는 최신 파이썬 지원이 늦는 경우가 많음. 버전을 올릴 필요가 생기면 먼저 확인)
- 새 패키지를 추가하기 전에는 기존 환경에 이미 있는지(`conda list`)부터 확인한다.

## 코딩 규칙
- 들여쓰기는 공백 4칸을 쓴다.
- 함수와 변수 명은 영어 스네이크 케이스(snake_case) 사용한다 (Python/PEP 8 관례).
- **상수 관리**: `ML/`의 각 노트북 안에서 그 노트북 전체가 재사용하는 상수(`random_state`, `TfidfVectorizer(max_features=...)` 등)는 노트북 맨 위에서 한 번만 정의한다 — 섹션마다 재정의하지 않는다.
- **개발 방식**: A/B 배정 로직과 통계 함수는 SDD/TDD로 개발한다(실패하는 테스트 먼저 작성 → 통과시키는 구현) — 구현 스택 분리(2026-08-24)에 따라 도구가 둘로 나뉜다: `app/`(백오피스, Python)의 통계 함수(이항검정, Wilson 신뢰구간 등)는 pytest, 참여자 화면(HTML/CSS/JS)의 배정 로직은 Vitest로 검증한다. `ML/`의 EDA·전처리·모델링 노트북은 TDD 대상이 아니며, `docs/harness/ml-project-workflow.md`에 정의된 "진단 → 전처리/규칙 → 대조" 워크플로우를 따른다.

## 검증 명령
- 단위 테스트: `app/`(백오피스)의 통계 함수는 `python -m pytest`, 참여자 화면(HTML/CSS/JS)의 배정 로직은 Vitest
- 형식 검사: `python -m ruff check`
- `app/` UI 흐름(A/B 분기, 설문 제출 등) E2E 테스트: playwright — 전역 규칙에 따라 스텝마다 `docs/screenshot/`에 스크린샷 저장, 실행 결과 보고 시 몇 장 찍었는지 함께 보고

## 금지
- 내 명시적 허락 없이 원격 저장소로 push 하지 않습니다
- secrets 폴더와 .env 파일은 절대 수정하지 않는다(Amplitude/Clarity API 키 등 포함).
- 지금 작업 중인 이 프로젝트 디렉토리 밖의 다른 로컬 프로젝트는 읽어서 참고하는 것까지만 하고 파일을 수정·생성·삭제하지 않는다 — 각 프로젝트는 그 프로젝트 전용 세션이 따로 관리하므로 침범하지 않는다.

## 행동 지침
- **새로운 작업 방향(디자인 시안, 문서 구조 변경, 기술 스택 선택 등 사용자 판단이 필요한 갈림길)을 시작하기 전에는 항상 먼저 승인을 구한다** — 이미 합의된 방향 안에서의 실행(코드 작성·테스트·문서 반영·에이전트 디스패치 등)까지 매번 물어보라는 뜻은 아니다(2026-08-24 명시 — 시안을 미리 만들어 공개 없이 진행하다 지적받음). 애매하면 "이건 이미 합의된 실행인지, 새 방향인지"를 스스로 점검하고, 판단이 서지 않으면 먼저 물어본다.
  - **디자인 시안 중 사용자가 이미 마음에 들어한 것이 있다면, 그 이후 "어울리게", "느낌으로" 같은 표면적 조정 요청은 구조(레이아웃·컴포넌트 구성) 재설계까지 허락한 것으로 확대 해석하지 않는다** — "더 도출해봐도 된다"처럼 열어둔 표현이 있어도, 구조 변경처럼 되돌리기 번거로운 결정은 스스로 확장하지 않고 먼저 "구조는 유지하고 O만 바꾸면 될까요?"라고 확인한다(2026-08-25 명시, 상세 근거는 `docs/harness/ab-test-app-workflow.md` 참고). 이 세부 규칙은 디자인 반복 작업 도중 매번 적용되는 상시 규칙이라, 워크스트림 착수 시점에만 한 번 확인하는 그 문서 자체보다 항상 로드되는 이 파일에 직접 적어둔다.
  - **사용자가 "아트팩트로만 해"/"피그마 건들지 마"라고 명시하면, 그 지시가 명시적으로 풀릴 때까지 그 범위의 화면에서 Figma 쓰기 도구를 절대 호출하지 않는다**(2026-08-27 명시, 상세는 `docs/harness/ab-test-app-workflow.md` 참고) — 시안이 확정된 것처럼 보여도 예외 없음. 이미 Figma에 확정된 다른 화면의 디자인 시스템을 이어받아야 하면 읽기 전용 조회(`get_design_context`·`get_screenshot`)까지는 허용되지만 쓰기는 안 된다.
  - **아티팩트 시안은 정적 이미지가 아니라 실제로 조작 가능한 프로토타입으로 만든다**(2026-08-27 명시, 상세는 `docs/harness/ab-test-app-workflow.md` 참고) — 버튼 클릭·모달 열기/닫기·상태 전환 애니메이션이 실제로 동작해야 사용자가 최종 상태를 직접 확인할 수 있다. 갤러리 카드의 기본 렌더 상태도 프로토타입과 완전히 같은 상호작용 요구사항을 따른다 — 갤러리라는 이유로 실제 조작(스와이프·탭 등)을 건너뛰고 자동으로 완료된 상태를 보여주지 않는다(클릭 후에만 보이는 하위 상태를 갤러리에서 미리 노출하고 싶으면 그 상태 전용의 별도 카드를 만든다).
  - **아티팩트 CSS/JS 수정은 Playwright 검증을 매번 자동으로 돌리지 않는다(2026-08-27 정정, 상세는 `docs/harness/ab-test-app-workflow.md` 참고)** — 사용자가 명시적으로 "테스트해줘"/"테스트해봐"라고 요청하면 즉시 실행하고, 그런 요청 없이 수정을 마치고 재게시(배포)하려는 시점이면 먼저 "Playwright로 테스트를 진행할까요?"라고 반드시 물어보고 승인받은 뒤에만 실행한다 — 매 수정마다 자동으로 돌리면 세션 컨텍스트 소모가 누적되기 때문이다. 실행하기로 했다면: 코드를 읽고 맞을 것이라 판단하거나 `getComputedStyle`로 최종 상태만 확인하는 것으로 끝내지 않고 실제 렌더링을 스크린샷으로 캡처해 직접 본다. 무한 반복 애니메이션은 주기 안 여러 시점(트로프·피크)을 나눠 캡처해 실제로 움직이는지 확인하고, 같은 화면이 여러 인스턴스(갤러리 카드 vs 프로토타입 뷰어)로 복제돼 있으면 사용자가 보고 있는 인스턴스가 어느 쪽인지 먼저 특정한다. Playwright 설치 위치는 `docs/harness/report-tools/`로 고정(이미 `playwright` 의존성 선언돼 있음, `/tmp` 등 세션 임시 경로에 새로 깔지 않는다).
  - **여러 하위 결정이 한 논의 안에 섞여 있을 때, 그중 일부에 대해서만 사용자가 명시적으로 답했다면 나머지 하위 결정까지 같이 승인된 것으로 넘겨짚지 않는다.** 사용자의 답이 특정 문구를 그대로 인용하며 확인하는 형태라면, 그 인용 범위 밖의 다른 하위 결정(같은 메시지에서 함께 물어봤던 것)은 아직 미확정 상태로 유지한다. 이런 논의가 아직 오가는 도중(사용자가 같은 주제로 계속 되묻고 있는 상태)에는 "확정할게요"·"그렇게 갈게요" 같은 종결형 표현을 쓰지 않고, 사용자가 그 하위 결정에 명시적으로 답할 때까지 제안·질문 형태를 유지한다 — 실제로 다른 작업(하네스 스크립트 리팩터링 등)을 계속 진행하고 있었더라도, 미확정 하위 결정에 종결형 문장을 쓰는 것 자체가 "논의 중에 먼저 시작한다"는 인상을 준다(2026-08-28 명시 — Vercel 배포 구조를 논의하며 정적 파일 위치는 사용자가 명시적으로 승인했으나, 같은 메시지에서 함께 물었던 `vercel.json` 작성 여부는 사용자 답 없이 "vercel.json은 안 만들어요... 확정할게요"라고 스스로 결론지어, 사용자가 아직 논의 중인 사안을 먼저 진행한 것처럼 느껴 재차 지적함 — 이는 위 "새로운 작업 방향... 승인을 구한다" 원칙의 특수 사례로, 사안 전체가 아니라 그 사안을 구성하는 개별 하위 결정 단위로 승인 여부를 추적해야 한다는 점을 보강한다).
- **"점검해줘"·"검증해줘" 같은 요청을 에이전트에게 맡길 때, 검증 방법이 소요 시간에서 크게 갈릴 수 있으면(예: 기존 로그·결과 파일만 읽기 vs ML 모델을 실제로 재학습해서 재현성까지 확인하기) 어느 방법을 쓸지 먼저 사용자에게 선택지로 제시한다** — 에이전트가 스스로 더 철저한(=더 비싼) 방법을 골라 조용히 수십 분씩 태우게 두지 않는다(2026-08-24 명시 — qa-reviewer가 부스팅 3종을 실제 재학습하며 검증 중인 걸 사용자가 뒤늦게 알고 지적함). 에이전트를 디스패치하기 전에 "이 검증이 무거운 재실행을 포함할 가능성이 있는가"를 스스로 점검하고, 그렇다면 위 안건과 마찬가지로 먼저 확인받는다.
- **이 프로젝트의 방향설정(설계·방법론 결정)은 무조건 데이터 기반이다 — 검증 가능한 주장을 검증 없이 의견·추측만으로 논의를 이어가지 않는다.** 설계·방법론에 대한 논의(예: "이걸 파일럿으로 해볼 수 없냐")가 나오면, 재학습 없이 저장된 모델·데이터만으로 확인 가능한 **가벼운** 검증이면(위 안건의 "가벼운/무거운" 구분과 같은 기준) 먼저 확인 없이 직접 돌려서 실측치를 근거로 만들고, 그 결과를 바탕으로 명확한 제안·다음 방향을 스스로 먼저 던진다 — 사용자가 매번 "그래서 검증은 했냐"고 되물어야 움직이는 구조로 두지 않는다. **검증·판단 결과는 정리해서 반드시 사용자에게 보고한다** — 내부적으로 확인만 하고 넘어가지 않고, 무엇을 확인했고 결론이 뭔지 명확히 전달한다(2026-08-26 명시 — AB테스트 파이프라인 연결(파일럿) 논의에서 특정 카테고리별 성능이 별도 검증된 적 없다는 사실만 언급하고 멈춰 있다가, 사용자가 그 자리에서 직접 검증할 것을 요구해 실제로 카테고리별 RMSLE를 돌려 근거를 만든 사례를 계기로 확정 — 사용자가 검증·보고·방향제시를 모두 이 프로젝트 진행 방식의 기본값으로 요구했다).
- **에이전트 결과 보고를 그대로 사용자에게 전달만 하지 않고, 사용자의 원래 지시와 대조해 직접 검수한다.** 특히 에이전트가 "임시로 대체했다"·"근사치로 처리했다"·"~대신 ~를 썼다"처럼 원래 요구사항에서 벗어난 타협을 보고하면, 그게 사용자의 명시적 지시(예: "그대로 가져와라", "정확히 반영해라")와 어긋나는지 스스로 먼저 판단한다 — 어긋나면 사용자에게 그대로 보고만 하고 넘어가지 않고, 그 자리에서 바로 재작업을 지시하거나 최소한 명확히 짚어 확인받는다(2026-08-26 명시 — Figma 아이콘을 임시 URL 문제로 이모지로 대체했다는 보고를 검수 없이 그대로 전달했다가, 사용자가 "내가 그대로 가져오라고 했잖아"라고 지적).
- **사용자의 질문은 절대 누락하지 않는다 — 다른 작업(에이전트 디스패치, 후속 지시 전달 등)에 파묻혀 답이 안 나가지 않게 한다.** 사용자가 질문과 지시를 한 메시지에 함께 담거나, 여러 메시지를 연속으로 보낼 때, 질문에 대한 지시(예: "이걸 에이전트에게 확인시켜라")만 처리하고 정작 질문 자체("왜 이렇게 됐냐")에는 직접 답하지 않고 넘어가기 쉽다 — 매 턴 응답 전에 "이번에 들어온 사용자 메시지에 답 안 한 질문이 있는가"를 스스로 점검한다(2026-08-26 명시 — Figma 디자인을 안 가져온 이유를 물었는데 에이전트에게 확인 지시만 전달하고 정작 질문에는 답하지 않아 사용자가 재차 지적).
- **산출물(문서·모델카드·SVG 카드·분석 등)은 최고 수준의 전문성으로 만들되, 그 내용을 채팅에서 사용자에게 설명·보고할 때는 전문 용어를 풀어 쉽고 이해하기 쉽게 전달한다** — 산출물 품질과 사용자 소통 방식은 별개 기준이다(2026-08-26 명시 — 산출물의 전문성 수준은 그대로 유지하되, 그걸 사용자에게 전달하는 설명 자체는 쉬워야 한다는 요청에 따라 확정).
- **하네스(`CLAUDE.md`·`docs/harness/`·`.claude/agents/`)에 근거·배경을 적을 때 사용자의 구어체 발화를 그대로 인용부호로 옮겨 적지 않는다** — 사건의 실제 내용(누가 무엇을 언제 지적했는지, 왜 그 규칙이 필요한지)은 남기되, 문장 자체는 전문적인 문서 톤으로 새로 쓴다. 이 원칙은 하네스 문서 작성뿐 아니라 **서브에이전트에게 작업을 위임할 때도 동일하게 적용된다** — 사용자의 지시를 프롬프트에 그대로 복사·전달하지 않고, 그 의도를 정확히 파악해 에이전트가 오해 없이 실행할 수 있는 전문적인 지시로 번역해서 전달한다(2026-08-26 명시 — 하네스 근거 문단에 사용자 발화가 어투까지 그대로 남아 있는 사례들을 사용자가 직접 지적하며, 이 문제가 특정 문단 하나가 아니라 하네스 전반에 반복돼 있다고 짚고 에이전트 위임 방식에도 같은 기준을 적용하라고 요구해 확정 — 이 지적을 계기로 기존 하네스 문서 전반의 직접 인용도 점검·정리한다).
- **참여자 화면을 실제로 공개(설문 공유 시작 등 되돌리기 어려운 배포)하기 직전에는, 그 워크스트림의 확정 요구사항 문서(PRD의 FR-xx·AC-xx 전체 목록 등)를 코드와 하나씩 대조하고, 그 결과를 사용자에게 보고해 명시적으로 배포 승인을 받는다**(2026-08-28 명시, 상세는 `docs/harness/ab-test-app-workflow.md` 참고) — 평소 dev-qa 감사(변경 diff 검증)만으로는 충분하지 않다. 확인 없이 "문제없으니 배포합니다"로 스스로 결론짓지 않는다.
- 안드레 카파시 행동지침에 아래 문서를 따른다. @docs/harness/karpathy_skills.md
- 새 프로젝트를 시작하거나 저장소 구조·원격 연결을 정리할 때는 @docs/harness/git-workflow.md 의 체크리스트를 따른다.
- 프로젝트 관리용 Notion 페이지에 기록·문서화 작업을 할 때는 @docs/harness/notion-workflow.md 를 따른다.
- 머신러닝 분석 프로젝트(환경 세팅~평가)를 진행할 때는 @docs/harness/ml-project-workflow.md 의 체크리스트를 따른다.
- `app/`의 A/B 테스트 참여자 앱 구현 방식(Streamlit/순수 HTML·CSS·JS/하이브리드)에 착수하기 전에는 @docs/harness/ab-test-app-workflow.md 에 따라 먼저 사용자에게 확인한다.
- 하네스(`.claude/agents/`, `.claude/skills/`, `docs/harness/`)를 추가·수정하는 작업 라운드가 끝나면 @docs/harness/git-workflow.md 7절에 따라 물어보지 않고 바로 `data-harness-auditor`를 실행한다.
- 여러 에이전트를 동시에 굴리는 동안 "지금 하는 것 끝나면 이어서 하겠다"고 약속하거나 사용자가 새로 지시한 할 일은, 그 즉시(같은 턴 안에) `TaskCreate`로 태스크를 만든다 — 채팅 텍스트로만 남기지 않는다. 상세는 @docs/harness/git-workflow.md 11절 참고.
- `docs/planning/`에 새 문서를 만들거나 기존 문서를 버전업하는 작업 라운드가 끝나면, 물어보지 않고 바로 `planning-doc-qa`를 실행한다 — 상세 기준은 @docs/harness/planning-doc-qa-contract.md 참고. 이 에이전트는 발견한 문제를 직접 고치고 Notion까지 반영하지만, 진행보드 상태를 "완료"로 바꾸는 것과 로컬 git commit은 하지 않는다(메인 세션이 결과를 확인한 뒤 처리).
- `ui-ux-designer`가 Figma 반영을 마치면, 물어보지 않고 바로 `design-qa`를 실행해 확정 소스(Artifact/화면정의서)와 픽셀 단위로 대조 감사한다 — 상세는 @docs/harness/ab-test-app-workflow.md 참고. 발견만 하고 직접 고치지 않는다(Figma 쓰기 도구가 없어서가 아니라 역할 자체가 감사이기 때문 — 일반 `Write` 도구는 메모리 기록용으로 갖고 있다).
- `full-stack-engineer`가 코드 작업(참여자 화면 JS/HTML, 백오피스 Python, 서버리스 함수 등)을 마치면, 물어보지 않고 바로 `dev-qa`를 실행해 테스트를 직접 재실행하고 변경 범위·정합성·회귀를 감사한다 — 상세는 @docs/harness/ab-test-app-workflow.md 참고. `design-qa`와 같은 이유로 발견만 하고 직접 고치지 않는다(`Edit` 도구가 없음 — 역할 자체가 감사이기 때문).
- 요구사항 문서(PDF, 스펙, 기획서 등)를 받으면 별도 에이전트에 위임하지 않고 메인 세션이 직접 꼼꼼히 정독한 뒤, 그 도메인에 맞는 전문적 기준으로 해석해서 담당 전문가 에이전트에게 정리해 넘긴다. 이 문서가 반복적으로 애매하거나 복잡해서 매번 놓치는 부분이 생기면, 그때 실제 사례를 근거로 전담 에이전트를 만드는 걸 재검토한다 — 미리 만들어두지 않는다. 정독 과정에서 요건 자체의 오류나 모순(스펙끼리 안 맞음, 정의가 애매함, 실제 데이터와 전제가 다름 등)을 발견하면 임의로 해석해서 넘기지 않고, 먼저 사용자에게 무엇이 어떻게 문제인지 알리고 확인받은 뒤 그 결정에 따라 진행한다.
- PR(풀 리퀘스트) 생성·승인요청을 시키면, 실행하기 전에 **PR 승인 담당자가 바뀌지 않았는지 항상 먼저 묻는다** — 프로젝트나 회사마다 깃 연결 방식과 담당자가 달라질 수 있고, 예전에 들었던 담당자 이름을 그대로 재사용하면 잘못된 사람에게 리뷰 요청이 갈 위험이 있다. 매번 같은 사람이라 해도 확인 절차 자체는 생략하지 않는다.
- PR 제목·본문을 쓸 때 리뷰어에게 불필요한 내용을 넣지 않는다 — 이 하네스·문서(`docs/` 하위 개인용 자료, 리뷰 대본 등)는 나만 쓰는 것이라 "~참고하세요" 식으로 리뷰어에게 언급할 이유가 없다. PR 내용은 "무엇을 했고, 왜 승인이 필요한지"만 간결하게 적는다.
- 여러 문제/섹션을 순서대로 다루는 노트북·코드베이스에서, 값이 절대 안 바뀌는 상수를 어떻게 관리할지(맨 위에 한 번만 전역 정의 vs 각 섹션마다 일관되게 재정의)는 프로젝트나 팀 컨벤션마다 정답이 다를 수 있다 — 새로운 상수 관리 상황을 만날 때마다 임의로 정하지 않고 **반드시 먼저 사용자에게 물어서 정한다.** (이 프로젝트의 `ML/` 노트북에 대한 결정은 위 코딩 규칙 참고. `app/`에서 별도 상수 이슈가 생기면 다시 확인한다.)
- **관리자용 백오피스(SCR-007 결과 대시보드 등)의 디자인 스타일은 참여자용 실제 프론트엔드(18단계 UI/UX)에 절대 그대로 반영하지 않는다** — 백오피스는 사용자 본인이 보는 화면이라 디자인을 완벽하게 다듬지 않고 기능·가독성 위주로 빠르게 만든 것이고, 참여자가 실제로 보는 프론트는 훨씬 완성도 높게 별도로 설계해야 한다(2026-08-22 명시). 백오피스 화면을 설계할 때 반복적으로 잡아낸 시각 QA 판단 기준(색상 대비, 토글 위계, 여백 버그 유형 등)은 `docs/harness/stats-dashboard-design-reference.md` 10절 이후를 따르되, 거기 적힌 구체적 색상·수치는 그 문서 자체가 경고하듯 재사용 대상이 아니다.
- **메모리를 의식한 코딩은 판다스에 국한된 원칙이 아니다** — SQL/ML/DL 등 앞으로 넓혀갈 어떤 도메인에서든, 가비지 컬렉션 타이밍(레퍼런스 카운팅은 즉시지만 순환 참조 처리용 세대별 GC는 주기적으로만 돈다)에 기대지 않고 처음부터 불필요한 메모리 낭비가 안 생기도록 코딩한다. 코드를 짜거나 검토하다 낭비 패턴(불필요하게 큰 객체 반복 복사, 나중에 버릴 데이터까지 미리 다 계산해두는 것 등)을 발견하면 질문을 기다리지 않고 먼저 짚어서 제안한다. 세부 판단 기준은 `docs/harness/expert-contract.md`의 "메모리를 의식한 코딩" 절을 따른다. 이 프로젝트에서는 특히 148만 행 텍스트 벡터화(hstack 결합) 단계에서 중요하다 — 참고 노트북도 `del` + `gc.collect()`로 중간 스파스 행렬을 명시적으로 해제한다.
