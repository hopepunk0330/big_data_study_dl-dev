# 프로젝트 안내

## 개요
딥러닝 교재(chap01~10)를 따라가며 실습 노트북을 쌓는 학습용 저장소. `pandas_dev` → `ml-dev` → `dl-dev`로 이어지는 학습 시리즈의 세 번째 프로젝트다.

- 원격 저장소: https://github.com/hopepunk0330/big_data_study_dl-dev
- **교재 PDF는 이 저장소 밖(`C:\big21\딥러닝 교재`, chap01~10.pdf)에 있고 저장소에 포함하지 않는다** — 저작권 자료이며 84MB로 저장소를 무겁게 만들기 때문. `.gitignore`의 `*.pdf`가 실수로 커밋되는 것을 막는다. 이 폴더는 읽기 참고만 하고 수정·이동·삭제하지 않는다(아래 금지 항목).

**이 프로젝트는 아직 별도의 진행상황·기획 관리 툴을 쓰지 않는다**(2026-09-01 확인) — 이전 프로젝트의 Notion 페이지는 그 프로젝트 전용이므로 여기서 참조하지 않는다. 나중에 관리 툴(Notion 등)을 도입하면 그 시점에 링크를 이 문단에 적어 넣는다(`docs/harness/git-workflow.md` 1-1절). 그때까지 `notion-workflow.md`는 이식된 상태로 두되 적용 대상이 없다.

`.claude/`와 `docs/harness/`에는 pandas_dev → ml-dev → (Mercari 가격예측) → dl-dev로 이식된 재사용 멀티에이전트 하네스가 있다 — 도메인별 전문가 에이전트(`dl-expert` 등, 최상위 전문가 페르소나 — 명시적 요청 시 완성된 구현까지 직접 수행), 크로스도메인 검증 에이전트(`qa-reviewer`), 하네스 자체 정합성 감사 에이전트(`data-harness-auditor`)는 `.claude/agents/`에, 사람이 직접 읽고 관리하는 공용 규칙·레퍼런스 문서는 숨김 폴더가 아닌 `docs/harness/`에, 노트북 스타일 가이드는 `.claude/skills/notebook-toolkit`에 둔다. 포크 계보는 `docs/harness/HARNESS_LOG.md` 참고.

**하네스는 이전 프로젝트에서 필요해서 통째로 이식해온 자산이다 — 지금 이 프로젝트에서 당장 안 쓰는 문서·스크립트·에이전트라도 임의로 삭제하지 않는다.** A/B 테스트·프론트엔드·보고서 관련 자산(`ab-test-app-workflow.md`, `report-tools/`의 검증 스크립트, `ui-ux-designer`·`dev-qa` 등)은 해당 워크스트림을 시작할 때 그대로 쓴다.

**이 프로젝트에서 `.claude/agent-memory/`는 git 추적 상태로 그대로 둔다**(2026-09-01 결정) — `memory-protocol.md`와 `git-workflow.md` 2절은 이 폴더를 `.gitignore`로 빼도록 규정하지만, 이 저장소에서는 사용자가 추적 유지를 명시적으로 선택했다. `.gitignore`에 항목은 있으나 파일이 이미 추적 중이라 실효가 없는 상태이며, **이는 방치된 오류가 아니라 의도된 결정이다 — `git rm --cached`로 "고치지" 않는다.** 대신 학습 이력이 원격에 올라간다는 점을 인지하고, 민감한 내용을 메모리에 적지 않는다.

## 폴더 구조
```
.claude/      # 하네스 — 에이전트, 스킬, 에이전트 메모리
docs/harness/ # 하네스 — 공용 규칙·레퍼런스 문서, 다이어그램 갤러리, report-tools
test.ipynb    # 환경 확인용(PyTorch/CUDA 인식, 기본 라이브러리 import)
```
챕터별 실습 노트북의 폴더 구조(예: `DL/01_*.ipynb` 형태로 갈지)는 아직 정하지 않았다 — 첫 챕터 실습을 시작할 때 확정한다.

## 개발 환경
- 가상환경: **venv** (이 학습 시리즈 관례 — ML은 Anaconda, DL은 venv). `uv`로 생성·관리한다: `uv venv --python 3.11 --seed`
- Python 버전: **3.11** (최신 버전을 기본값으로 삼지 않는다 — 딥러닝 프레임워크는 최신 파이썬 지원이 늦는 경우가 많다. 버전을 올릴 필요가 생기면 먼저 확인). 현재 `.venv`는 3.11.16.
- 프레임워크: **PyTorch**. `test.ipynb`의 실행 기록 기준 PyTorch 2.5.1 / CUDA 11.8 / GPU: NVIDIA GeForce GTX 1660 SUPER.
  - **현재 `.venv`에는 PyTorch가 설치돼 있지 않다**(ipykernel만 설치됨). `test.ipynb`의 출력은 다른 환경에서 실행된 기록이다.
  - **PyTorch 설치는 수업에서 직접 진행한다 — Claude가 임의로 설치하지 않는다**(2026-09-01 명시). CUDA 빌드 선택(cu118/cu121/CPU)이 학습 환경 구성의 일부이므로, 요청받기 전에 앞질러 설치하지 않는다.
- 새 패키지를 추가하기 전에는 기존 환경에 이미 있는지(`uv pip list`)부터 확인한다.
- VS Code는 `.vscode/settings.json`의 `python.defaultInterpreterPath`로 `.venv`를 자동 인식한다.

## 코딩 규칙
- 들여쓰기는 공백 4칸을 쓴다.
- 함수와 변수 명은 영어 스네이크 케이스(snake_case) 사용한다 (Python/PEP 8 관례).
- **상수 관리**: 각 노트북 안에서 그 노트북 전체가 재사용하는 상수(`random_state`, `BATCH_SIZE`, `LEARNING_RATE` 등)는 노트북 맨 위에서 한 번만 정의한다 — 섹션마다 재정의하지 않는다.
- **재현성**: 학습 코드에는 시드를 고정한다(`torch.manual_seed`, `numpy.random.seed` 등) — 실행할 때마다 결과가 달라지면 챕터 간 비교가 무의미해진다.
- **개발 방식**: 교재 실습 노트북은 TDD 대상이 아니다. 별도의 유틸리티 모듈(전처리 함수, 커스텀 Dataset/Metric 등)을 만들게 되면 그때 pytest로 검증한다.

## 검증 명령
- 단위 테스트: `python -m pytest` (유틸리티 모듈이 생겼을 때)
- 형식 검사: `python -m ruff check`
- 환경 확인: `test.ipynb` 첫 셀 (PyTorch 버전·CUDA 가용성·GPU 인식)

## 금지
- 내 명시적 허락 없이 원격 저장소로 push 하지 않습니다
- secrets 폴더와 .env 파일은 절대 수정하지 않는다.
- **`C:\big21\딥러닝 교재` 폴더는 읽기 참고까지만 하고 파일을 수정·생성·삭제·이동하지 않는다**(2026-09-01 명시) — 교재 원본 자료이며 이 저장소의 관리 대상이 아니다.
- 지금 작업 중인 이 프로젝트 디렉토리 밖의 다른 로컬 프로젝트는 읽어서 참고하는 것까지만 하고 파일을 수정·생성·삭제하지 않는다 — 각 프로젝트는 그 프로젝트 전용 세션이 따로 관리하므로 침범하지 않는다.
- **이식해온 하네스 자산(`.claude/`, `docs/harness/`)을 "지금 안 쓴다"는 이유로 삭제하지 않는다**(2026-09-01 명시) — 필요해서 가져온 것이므로, 정리가 필요하다고 판단되면 삭제 대신 먼저 사용자에게 확인한다.

## 행동 지침
아래 지침 중 A/B 테스트·참여자 화면·Figma·보고서 워크스트림을 전제한 항목은, 이 프로젝트에서 그 워크스트림을 실제로 시작하는 시점부터 그대로 적용한다(이식된 규칙이므로 삭제하지 않는다).

- **새로운 작업 방향(디자인 시안, 문서 구조 변경, 기술 스택 선택 등 사용자 판단이 필요한 갈림길)을 시작하기 전에는 항상 먼저 승인을 구한다** — 이미 합의된 방향 안에서의 실행(코드 작성·테스트·문서 반영·에이전트 디스패치 등)까지 매번 물어보라는 뜻은 아니다. 애매하면 "이건 이미 합의된 실행인지, 새 방향인지"를 스스로 점검하고, 판단이 서지 않으면 먼저 물어본다.
  - **디자인 시안 중 사용자가 이미 마음에 들어한 것이 있다면, 그 이후 "어울리게", "느낌으로" 같은 표면적 조정 요청은 구조(레이아웃·컴포넌트 구성) 재설계까지 허락한 것으로 확대 해석하지 않는다** — "더 도출해봐도 된다"처럼 열어둔 표현이 있어도, 구조 변경처럼 되돌리기 번거로운 결정은 스스로 확장하지 않고 먼저 "구조는 유지하고 O만 바꾸면 될까요?"라고 확인한다(2026-08-25 명시, 상세 근거는 `docs/harness/ab-test-app-workflow.md` 참고).
  - **사용자가 "아트팩트로만 해"/"피그마 건들지 마"라고 명시하면, 그 지시가 명시적으로 풀릴 때까지 그 범위의 화면에서 Figma 쓰기 도구를 절대 호출하지 않는다**(2026-08-27 명시) — 시안이 확정된 것처럼 보여도 예외 없음. 읽기 전용 조회(`get_design_context`·`get_screenshot`)까지는 허용되지만 쓰기는 안 된다.
  - **아티팩트 시안은 정적 이미지가 아니라 실제로 조작 가능한 프로토타입으로 만든다**(2026-08-27 명시) — 버튼 클릭·모달 열기/닫기·상태 전환 애니메이션이 실제로 동작해야 사용자가 최종 상태를 직접 확인할 수 있다.
  - **아티팩트 CSS/JS 수정은 Playwright 검증을 매번 자동으로 돌리지 않는다**(2026-08-27 정정) — 사용자가 명시적으로 요청하면 즉시 실행하고, 그런 요청 없이 재게시하려는 시점이면 먼저 물어보고 승인받은 뒤에만 실행한다. 실행하기로 했다면 `getComputedStyle`로 끝내지 않고 실제 렌더링을 스크린샷으로 캡처해 직접 본다. Playwright 설치 위치는 `docs/harness/report-tools/`로 고정한다.
  - **여러 하위 결정이 한 논의 안에 섞여 있을 때, 그중 일부에 대해서만 사용자가 명시적으로 답했다면 나머지 하위 결정까지 같이 승인된 것으로 넘겨짚지 않는다.** 논의가 아직 오가는 도중에는 "확정할게요"·"그렇게 갈게요" 같은 종결형 표현을 쓰지 않고, 사용자가 그 하위 결정에 명시적으로 답할 때까지 제안·질문 형태를 유지한다(2026-08-28 명시 — 사안 전체가 아니라 개별 하위 결정 단위로 승인 여부를 추적해야 한다).
- **"점검해줘"·"검증해줘" 같은 요청을 에이전트에게 맡길 때, 검증 방법이 소요 시간에서 크게 갈릴 수 있으면(예: 기존 로그·결과 파일만 읽기 vs 모델을 실제로 재학습해서 재현성까지 확인하기) 어느 방법을 쓸지 먼저 사용자에게 선택지로 제시한다** — 에이전트가 스스로 더 철저한(=더 비싼) 방법을 골라 조용히 수십 분씩 태우게 두지 않는다(2026-08-24 명시). 딥러닝에서는 특히 중요하다 — 학습 재실행은 GPU 시간을 크게 소모한다.
- **이 프로젝트의 방향설정(설계·방법론 결정)은 무조건 데이터 기반이다 — 검증 가능한 주장을 검증 없이 의견·추측만으로 논의를 이어가지 않는다.** 재학습 없이 저장된 모델·데이터만으로 확인 가능한 **가벼운** 검증이면 먼저 확인 없이 직접 돌려서 실측치를 근거로 만들고, 그 결과를 바탕으로 명확한 제안·다음 방향을 스스로 먼저 던진다. **검증·판단 결과는 정리해서 반드시 사용자에게 보고한다**(2026-08-26 명시).
- **에이전트 결과 보고를 그대로 사용자에게 전달만 하지 않고, 사용자의 원래 지시와 대조해 직접 검수한다.** 특히 에이전트가 "임시로 대체했다"·"근사치로 처리했다"처럼 원래 요구사항에서 벗어난 타협을 보고하면, 그게 사용자의 명시적 지시와 어긋나는지 스스로 먼저 판단한다 — 어긋나면 그대로 전달만 하고 넘어가지 않고 바로 재작업을 지시하거나 명확히 짚어 확인받는다(2026-08-26 명시).
- **사용자의 질문은 절대 누락하지 않는다 — 다른 작업(에이전트 디스패치, 후속 지시 전달 등)에 파묻혀 답이 안 나가지 않게 한다.** 매 턴 응답 전에 "이번에 들어온 사용자 메시지에 답 안 한 질문이 있는가"를 스스로 점검한다(2026-08-26 명시).
- **산출물(문서·모델카드·SVG 카드·분석 등)은 최고 수준의 전문성으로 만들되, 그 내용을 채팅에서 사용자에게 설명·보고할 때는 전문 용어를 풀어 쉽고 이해하기 쉽게 전달한다** — 산출물 품질과 사용자 소통 방식은 별개 기준이다(2026-08-26 명시).
- **하네스(`CLAUDE.md`·`docs/harness/`·`.claude/agents/`)에 근거·배경을 적을 때 사용자의 구어체 발화를 그대로 인용부호로 옮겨 적지 않는다** — 사건의 실제 내용은 남기되, 문장 자체는 전문적인 문서 톤으로 새로 쓴다. **서브에이전트에게 작업을 위임할 때도 동일하게 적용된다** — 사용자의 지시를 프롬프트에 그대로 복사·전달하지 않고, 의도를 정확히 파악해 에이전트가 오해 없이 실행할 수 있는 전문적인 지시로 번역해서 전달한다(2026-08-26 명시).
- **되돌리기 어려운 배포·공개 직전에는 그 워크스트림의 확정 요구사항 문서를 코드와 하나씩 대조하고, 그 결과를 사용자에게 보고해 명시적으로 배포 승인을 받는다**(2026-08-28 명시) — 평소 감사(변경 diff 검증)만으로는 충분하지 않다. 확인 없이 "문제없으니 배포합니다"로 스스로 결론짓지 않는다.
- 안드레 카파시 행동지침에 아래 문서를 따른다. @docs/harness/karpathy_skills.md
- 새 프로젝트를 시작하거나 저장소 구조·원격 연결을 정리할 때는 @docs/harness/git-workflow.md 의 체크리스트를 따른다.
- 프로젝트 관리용 Notion 페이지에 기록·문서화 작업을 할 때는 @docs/harness/notion-workflow.md 를 따른다.
- 머신러닝·딥러닝 분석 프로젝트(환경 세팅~평가)를 진행할 때는 @docs/harness/ml-project-workflow.md 의 체크리스트를 따른다 — "진단 → 전처리/규칙 → 대조" 워크플로우, 3-way 분할·데이터 누수 방지·공정한 하이퍼파라미터 비교·모델+전처리기 저장 원칙은 딥러닝에도 그대로 적용된다.
- `app/`의 A/B 테스트 참여자 앱 구현 방식에 착수하기 전에는 @docs/harness/ab-test-app-workflow.md 에 따라 먼저 사용자에게 확인한다.
- 하네스(`.claude/agents/`, `.claude/skills/`, `docs/harness/`)를 추가·수정하는 작업 라운드가 끝나면 @docs/harness/git-workflow.md 7절에 따라 물어보지 않고 바로 `data-harness-auditor`를 실행한다.
- 여러 에이전트를 동시에 굴리는 동안 "지금 하는 것 끝나면 이어서 하겠다"고 약속하거나 사용자가 새로 지시한 할 일은, 그 즉시(같은 턴 안에) 태스크로 만든다 — 채팅 텍스트로만 남기지 않는다. 상세는 @docs/harness/git-workflow.md 11절 참고.
- `docs/planning/`에 새 문서를 만들거나 기존 문서를 버전업하는 작업 라운드가 끝나면, 물어보지 않고 바로 `planning-doc-qa`를 실행한다 — 상세 기준은 @docs/harness/planning-doc-qa-contract.md 참고.
- `ui-ux-designer`가 Figma 반영을 마치면, 물어보지 않고 바로 `design-qa`를 실행해 확정 소스와 픽셀 단위로 대조 감사한다 — 상세는 @docs/harness/ab-test-app-workflow.md 참고. 발견만 하고 직접 고치지 않는다.
- `full-stack-engineer`가 코드 작업을 마치면, 물어보지 않고 바로 `dev-qa`를 실행해 테스트를 직접 재실행하고 변경 범위·정합성·회귀를 감사한다 — 상세는 @docs/harness/ab-test-app-workflow.md 참고. 발견만 하고 직접 고치지 않는다.
- 요구사항 문서(PDF, 스펙, 기획서 등)를 받으면 별도 에이전트에 위임하지 않고 메인 세션이 직접 꼼꼼히 정독한 뒤, 그 도메인에 맞는 전문적 기준으로 해석해서 담당 전문가 에이전트에게 정리해 넘긴다. 정독 과정에서 요건 자체의 오류나 모순을 발견하면 임의로 해석해서 넘기지 않고, 먼저 사용자에게 무엇이 어떻게 문제인지 알리고 확인받은 뒤 그 결정에 따라 진행한다.
- PR(풀 리퀘스트) 생성·승인요청을 시키면, 실행하기 전에 **PR 승인 담당자가 바뀌지 않았는지 항상 먼저 묻는다** — 프로젝트나 회사마다 깃 연결 방식과 담당자가 달라질 수 있다. 매번 같은 사람이라 해도 확인 절차 자체는 생략하지 않는다.
- PR 제목·본문을 쓸 때 리뷰어에게 불필요한 내용을 넣지 않는다 — 이 하네스·문서는 나만 쓰는 것이라 리뷰어에게 언급할 이유가 없다. PR 내용은 "무엇을 했고, 왜 승인이 필요한지"만 간결하게 적는다.
- 여러 문제/섹션을 순서대로 다루는 노트북·코드베이스에서, 값이 절대 안 바뀌는 상수를 어떻게 관리할지는 프로젝트나 팀 컨벤션마다 정답이 다를 수 있다 — 새로운 상수 관리 상황을 만날 때마다 임의로 정하지 않고 **반드시 먼저 사용자에게 물어서 정한다.** (이 프로젝트의 노트북에 대한 결정은 위 코딩 규칙 참고.)
- **관리자용 백오피스의 디자인 스타일은 사용자에게 실제로 노출되는 프론트엔드에 그대로 반영하지 않는다** — 백오피스는 기능·가독성 위주로 빠르게 만든 것이고, 실제 사용자가 보는 화면은 훨씬 완성도 높게 별도로 설계해야 한다(2026-08-22 명시). 시각 QA 판단 기준은 `docs/harness/stats-dashboard-design-reference.md` 10절 이후를 따르되, 거기 적힌 구체적 색상·수치는 재사용 대상이 아니다.
- **메모리를 의식한 코딩은 판다스에 국한된 원칙이 아니다** — SQL/ML/DL 등 어떤 도메인에서든, 가비지 컬렉션 타이밍에 기대지 않고 처음부터 불필요한 메모리 낭비가 안 생기도록 코딩한다. 코드를 짜거나 검토하다 낭비 패턴을 발견하면 질문을 기다리지 않고 먼저 짚어서 제안한다. 세부 판단 기준은 `docs/harness/expert-contract.md`의 "메모리를 의식한 코딩" 절을 따른다. **딥러닝에서는 특히 GPU 메모리가 병목이다** — 배치 크기, 중간 텐서 보존(`detach()`·`no_grad()` 사용), 학습 루프 안의 불필요한 텐서 누적(로그용 텐서를 `.item()` 없이 리스트에 쌓는 등), `torch.cuda.empty_cache()` 타이밍을 의식한다. 이 환경의 GTX 1660 SUPER는 VRAM 6GB로 넉넉하지 않다.
